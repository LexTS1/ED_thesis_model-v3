"""Shared utilities namespace for model_v3.

No top-level shared package exists in the current model_v1 repository, so this
package intentionally stays minimal. If cross-version utilities are formalized
later, import them here instead of copying code into model_v3.
"""
